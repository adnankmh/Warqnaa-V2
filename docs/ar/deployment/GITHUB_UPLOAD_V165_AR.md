# رفع Warqna v165 إلى GitHub

- استخدم محتويات المجلد الداخلي وليس المجلد الخارجي نفسه.
- لا تحذف `.git`.
- شغّل `CHECK_V165_WINDOWS.bat` قبل Commit.
- شغّل Workflow: Build Android APK and AAB.
- ثبّت `warqna-v165-safe.apk` فقط.

يفضل حذف النسخة القديمة من الهاتف قبل تثبيت v165 لضمان عدم بقاء قاعدة بيانات WorkManager قديمة.
