# رفع Build 650

المصدر الكامل العام مناسب لمراجعة GitHub. لا ترفع الحزمة الخاصة أو .env أو بيانات SQLite أو سجلات التثبيت.

1. شغّل CHECK_WARQNA_WINDOWS.bat.
2. ارفع المصدر لفرع مراجعة في مستودعك وشغّل backend-ci وFlutter workflows وWindows installer validation.
3. لا تعتمد الإصدار على الإنتاج قبل نجاح PHPUnit وFlutter analyze/test/build وفحص رحلة تسجيل الدخول والغرفة والشراء والتفعيل على جهازين.
4. للإنتاج: نسخة احتياطية مستقلة لقاعدة البيانات، migrations بعد الاختبار، HTTPS، APP_DEBUG=false، جدولة Laravel كل دقيقة، عامل queue عند استخدام طابور غير sync، مفاتيح الدفع والإشعارات والتوقيع عبر أسرار البيئة.

المثبت المرفق للتشغيل المحلي على Windows/SQLite. لا يُستخدم لاستبدال قاعدة إنتاجية خارجية.
