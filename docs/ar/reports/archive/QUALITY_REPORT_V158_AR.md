# تقرير جودة Warqna v158

تم التحقق من الحزمة عبر:

- فحص علامات تعارض Git.
- فحص اتساق الإصدار `1.58.0+158` عبر Flutter وLaravel وGitHub Actions.
- اختبار ذاتي فعلي لمحلل `pubspec.lock` والتحقق من عدم بقاء `grep -A2` في Workflow Android.
- فحص الحفاظ على SQLite `:memory:` وSQLite URI وعدم تحويلهما إلى مسارات ملفات.
- اختبارات PHP معزولة فعلية لتطبيع صيغ الورق وتدفق مزايدة/حكم الطرنيب ودعم الحالة القديمة.
- فحص عقود Mobile API وPWA وكتالوج 12 لعبة والمتجر ذي 50 طاولة و40 ظهر ورق.
- فحص PHP Syntax لكل ملفات PHP في المشروع.
- فحص JSON وYAML وPython وShell/BAT بنيويًا.
- فحص عدم تضمين `.env` فعلي أو أسرار أو مفاتيح توقيع.
- فحص سلامة الملف المضغوط ورمز SHA-256.

لم يُشغّل `composer install` أو مجموعة `php artisan test` الكاملة داخل بيئة تجهيز الحزمة لأن Composer وحزم vendor غير متاحين فيها. GitHub Actions سيشغّل الاختبار التنفيذي الكامل بعد الرفع. كما سيجري بناء Flutter/Android النهائي داخل GitHub Actions.
