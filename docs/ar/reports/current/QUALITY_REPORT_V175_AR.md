# تقرير جودة Warqna v175

الإصدار: **1.75.0+175**

## الفحوص المنفذة محلياً

- نجح فحص `tools/validate_release.py` كاملاً.
- نجح فحص صياغة **296 ملف PHP** بواسطة `php -l` دون أخطاء.
- نجحت عقود الإصدارات V169 وV170 وV171 وV172 وV173 وV174 وV175.
- نجح فحص بنية ملفات Dart والسلاسل والأقواس، ونجحت عقود مراجع `AppController`.
- نجح فحص JSON وYAML وملف `pubspec.lock` وبنية GitHub Actions.
- تم التحقق من أن جدول XP يحتوي **100 قيمة دقيقة** مطابقة لملف Excel المرفق.
- تم التحقق من وجود عقد التوجيه المباشر الذي فشل سابقاً: `queueNavigationRoute` و`openPendingNavigationRoute` و`_prepareDirectInviteTransfer`.
- تم التحقق من استخدام صورة الباشا الحمراء الكاملة مع `BoxFit.contain` ومن عدم إظهار زر المعاينة لها.
- تم التحقق من إخفاء تبويب ألوان الطربوش من المتجر مع بقاء التوافق الرجعي للبيانات القديمة.

## حدود بيئة الفحص

لم تتوفر حزمة Flutter SDK ولا Composer/vendor في بيئة التجهيز، لذلك لم يتم تشغيل `flutter analyze` أو `flutter test` أو `php artisan test` تنفيذياً محلياً. ملفات GitHub Actions مهيأة لتشغيلها بعد رفع المشروع، والفحوص الساكنة والعقود المصدرية نجحت بالكامل.
