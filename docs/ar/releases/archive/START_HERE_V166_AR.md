# ابدأ من هنا — Warqna v166

الإصدار: `1.66.0+166`، مبني فوق النسخة المستقرة v165 التي عالجت انهيار WorkManager قبل تشغيل Flutter.

## قبل الرفع

1. احتفظ بنسخة مستقلة من v165 باسم **Stable Baseline**.
2. نفّذ Fetch ثم Pull من GitHub Desktop.
3. ألغِ أي Merge معلّق.
4. احتفظ بمجلد `.git` فقط، واستبدل بقية ملفات المستودع بمحتويات مجلد v166 الداخلي.
5. شغّل `CHECK_V166_WINDOWS.bat`.
6. ارفع Commit باسم: `Warqna v166 global voice social polish release`.

## البناء

بعد نجاح فحوص GitHub Actions شغّل يدويًا Workflow باسم `Build Android APK and AAB`، ثم حمّل Artifact باسم `warqna-v166-android` وثبّت `warqna-v166-safe.apk`.

## اختبار قبول سريع

- افتح APK وتأكد من عدم انهياره.
- أنشئ غرفة عادية وصوتية، وتحقق من الانتقال المباشر إليها.
- اسمح للميكروفون، ثم افتح شاشة حالة الصوت.
- جرّب رمي الورقة بنقرتين وبالسحب للأعلى.
- اخرج للرئيسية دون مغادرة الغرفة، ثم استخدم شريط العودة إلى اللعبة.
- اختبر دردشة اللعبة، دردشة الأصدقاء، الإيموجي والأصوات.
- أنهِ جولة وتحقق من تقرير النقاط.
- افتح البروفايل والمتجر والمعاينات وصفحة سياسة الخصوصية.


## تشغيل الإشعارات عند إغلاق التطبيق

راجع `VOICE_ANDROID_PUSH_SETUP_V166_AR.md`. الواجهة والمرسل البرمجي موجودان، لكن التشغيل الفعلي يحتاج قيم Firebase الخاصة بالمشروع داخل GitHub Variables وService Account داخل بيئة خادم Laravel.
