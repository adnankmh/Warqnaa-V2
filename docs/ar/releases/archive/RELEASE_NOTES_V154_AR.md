# ملاحظات إصدار Warqna v154

## إصلاح الدمج وتسجيل الدخول

- حل تعارض `main.dart` الذي ظهر عند Pull من GitHub.
- إزالة تظليل اسم دالة `login` بواسطة وسيط يحمل الاسم نفسه.
- توحيد الدخول المحلي الاحتياطي باستخدام `loginId` و`this.login(...)`.
- منع إدخال علامات تعارض Git إلى أي بناء جديد.

## سلامة الإصدار

- Flutter: `1.54.0+154`.
- Laravel: `WARQNA_VERSION=1.54.0` و`WARQNA_BUILD=154`.
- Android/iOS/Web Workflows تبني الإصدار 154.
- إضافة Migration لتسجيل v154 كأحدث إصدار نشط لكل منصة.

## الميزات المحفوظة

لم تُحذف ميزات v153. تبقى الحسابات المستقلة والصور الشخصية لكل لاعب، تحويل التوكنز، الألعاب العادية والصوتية، الغرف، الثيمات، اللغات، إعدادات الخط والأزرار، المتجر، الإيموجي، الأغلفة، الإدارة، الإعلانات، الخصوصية، الجلسات، البلاغات والنشر الإنتاجي ضمن المشروع.

## الفحوص الجديدة

- `tools/validate_release.py`.
- `CHECK_V154_WINDOWS.bat`.
- `check-v154.sh`.
- تشغيل فحص الحزمة داخل Workflows قبل Flutter Analyze والبناء.
