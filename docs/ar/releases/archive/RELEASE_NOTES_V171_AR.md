# ملاحظات إصدار Warqna v171

## الإصلاح الرئيسي

أزيل فحص GitHub Actions الهش الذي كان يبحث عن النص الحرفي:

```dart
onPressed: () => showFriends(context, controller)
```

بينما التنفيذ الصحيح في البروفايل كان يغلق النافذة أولًا ثم يفتح الأصدقاء داخل block callback. أصبح الفحص الآن يفهم الاستدعاءات الفعلية بدل الاعتماد على تنسيق سطر واحد.

## الحماية الجديدة

- ملف `tools/test_v171_controller_references.py`.
- رفض أي وسيط غير `controller` أو `widget.controller`.
- منع إعادة إدخال الفحص الحرفي القديم في Workflow.
- جعل عقد v170 قابلًا لإعادة الاستخدام في الإصدارات اللاحقة بدل ربطه برقم إصدار واحد.
