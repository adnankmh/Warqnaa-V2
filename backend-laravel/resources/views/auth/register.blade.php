@extends('layouts.app') @section('content')
@php $countries=config('countries',[]); unset($countries['IL']); @endphp
<div class="auth-card"><h2>تسجيل مستخدم جديد</h2><form method="post">@csrf<input name="username" placeholder="اسم مستخدم فريد" required><input name="email" type="email" placeholder="Email فعال" required><label>الدولة</label><div id="countryPreview" class="country-preview"></div><select name="country_code" onchange="updateCountryPreview(this)">@foreach($countries as $code=>$name)<option value="{{$code}}" data-flag="{{ flag_url($code) }}" {{$code==='PS'?'selected':''}}>{{ is_array($name) ? (($name['flag'] ?? '').' '.($name['ar'] ?? $code).' — '.($name['en'] ?? $code)) : $name }} - {{$code}}</option>@endforeach</select><input type="password" name="password" placeholder="Password" required><input type="password" name="password_confirmation" placeholder="Confirm Password" required><button class="btn primary">تسجيل</button></form></div>@endsection
