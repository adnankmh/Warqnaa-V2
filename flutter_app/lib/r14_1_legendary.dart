part of 'main.dart';

// B302 compatibility tombstone.
// R14.1's experimental legendary UI was retired before R14.2, but older
// Git repositories may still track this path. Keeping this intentionally
// empty part file overwrites stale historical copies so `flutter analyze`
// cannot fail on obsolete R14.1 declarations after an in-place upgrade.
